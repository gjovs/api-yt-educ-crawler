{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "JavaScript Debug Terminal",
            "request": "never",
            "cwd": "${workspaceFolder}"
        }
    ]
}
